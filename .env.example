# Optional absolute API origin (no trailing slash). Empty = same-origin `/api/v1/...`.
# Read at the SvelteKit composition root via `$env/static/public` and passed into createApiV1Client.
PUBLIC_API_BASE_URL=
